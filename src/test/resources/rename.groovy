/**
 * Renames the target folder with the delivered files.
 *
 * @param communicationConfirmationData the data from the communication confirmation in the form of a JSON object represented as a Map
 * @param targetFolder the folder with the delivered files
 * @return true if the folder was renamed and false otherwise
 */
String rename(Map communicationConfirmationData) {
  // rename the target folder


  return "new name";
}
